package templates

import "github.com/seanmorton/hledger-htmx/internal/hledger"

templ Register(entries []hledger.RegisterEntry) {
  <table border="1">
    <tbody>
      for _, entry := range entries {
        <tr>
        <td>{ entry.Date }</td>
        <td>{ entry.Description}</td>
        <td>{ entry.Amount}</td>
        </tr>
      }
    </tbody>
  </table>
}
